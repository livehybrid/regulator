// Splunk's own preset, so the JSX, the styled-components displayNames and the
// browser targets match every other Splunk app rather than being this repo's
// private dialect.
module.exports = {
    presets: ['@splunk/babel-preset'],
};
